# PR #49390: debugger: Open correct pane for breakpoints

_Merged: 2026-02-24T11:52:18Z_
_PR: https://github.com/zed-industries/zed/pull/49390_


## Documentation Suggestions

### Summary
This change fixes how the debugger interacts with split panes by introducing a "persistent debug pane" behavior. The debugger now consistently shows the active debug line in one pane and avoids disrupting inactive tabs in other panes. This user-facing behavior change warrants brief documentation explaining how the debugger chooses which pane to use.

### Suggested Changes

#### 1. docs/src/debugger.md

- **Section**: New subsection after "Breakpoints" section
- **Change**: Add
- **Target keyword**: debugger split panes
- **Frontmatter**:
  ```yaml
  ---
  title: Debugger - Zed
  description: Debug code in Zed with the Debug Adapter Protocol (DAP). Breakpoints, stepping, variable inspection across multiple languages.
  ---
  ```
- **Links**: 
  - [Editing Code](./editing-code.md) (for split pane context)
  - [Tasks](./tasks.md) (related workflow tool)
  - [Terminal](./terminal.md) (related workflow tool)
  - https://zed.dev/features/debugger (marketing link for main debugger feature)
  
- **Suggestion**: Add the following subsection after the "Breakpoints" section:

  ```markdown
  ## Working with Split Panes
  
  > **Changed in Preview (v0.225).** See [release notes](/releases#0.225).
  
  When debugging with multiple split panes open, Zed shows the active debug line in one pane and preserves your layout in others. If you have the same file open in multiple panes, the debugger picks a pane where the file is already the active tab—it won't switch tabs in panes where the file is inactive.
  
  Once the debugger picks a pane, it continues using that pane for subsequent breakpoints during the session. If you drag the tab with the active debug line to a different split, the debugger tracks the move and uses the new pane.
  
  This ensures the debugger doesn't disrupt your workflow when stepping through code across different files.
  ```

- **Full-file brand pass**: Yes. The existing debugger.md file is well-written and meets brand guidelines. The only minor improvement needed is replacing "At present, you can:" with "You can:" in the Breakpoints section to be more direct. The rest of the file uses clear, factual language without marketing hype and follows the second-person present-tense style correctly.

- **Brand voice scorecard**:

  | Criterion            | Score | Notes |
  | -------------------- | ----- | ----- |
  | Technical Grounding  | 5     | Explains concrete behavior with specific examples |
  | Natural Syntax       | 5     | Clear, conversational flow without awkward phrasing |
  | Quiet Confidence     | 5     | States what happens without hedging or overselling |
  | Developer Respect    | 5     | Trusts reader to understand; explains "why" not just "what" |
  | Information Priority | 5     | Leads with practical impact, details follow |
  | Specificity          | 5     | Names exact behaviors: "picks a pane where file is already active tab" |
  | Voice Consistency    | 5     | Matches existing docs style: second person, present tense |
  | Earned Claims        | 5     | "Doesn't disrupt workflow" is demonstrated, not asserted |
  | **TOTAL**            | 40/40 |       |

  Pass threshold: all criteria 4+. ✓

### Notes for Reviewer

This is primarily a bug fix (issue #40602) that prevents the debugger from switching active tabs in panes where the file wasn't already active. The new "persistent debug pane" behavior is a side effect of the fix but is user-visible and worth documenting briefly. The suggested text is intentionally short—users will experience this as "the debugger stays out of my way" rather than needing to understand the implementation details.
